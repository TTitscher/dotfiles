# dotfiles



# intall

*todo: make a script out of that*

install
- sudo apt-get install python-neovim
- atom from https://atom.io/ `dpkg -i atom.deb`

link 
- `screens.sh` to .kde/Autostart
- atom/* to ~/.atom/
- zshrc to ~/.zshrc
- nvim to ~/.config/nvim

# additional tools

npm install -g svelte-language-server


